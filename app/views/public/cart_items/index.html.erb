<table class="table">
  <thead>
    <tr>
      <th>商品名</th>
      <th>単価（税込）</th>
      <th>数量</th>
      <th>小計</th>
    </tr>
  </thead>

  <tbody>
    <% @cart_items.each do |cart_item| %>
      <tr>
        <td><%= cart_item.item.name %></td>
        <td><%= cart_item.item.price %></td>
        <%= form_for(cart_item, url:public_cart_items_path(cart_item),method: :patch) do |f| %>
          <!--% sub_total = cart_item.count.to_i + cart.count.to_i %>-->

        <!--<td>%= sub_total %></td>   -->
        <td> 
        <!--%= f.select :count,(1..50) %>-->
        <%= f.submit "変更する"%>
        <% end %>
        </td>

        <!--<td>¥% @sum_price %></td>-->
        <td><%= link_to "削除する", public_cart_items_path(cart_item.id), method: :delete %></td>
      </tr>
    <% end %>
  </tbody>
</table>