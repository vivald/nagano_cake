<div class="container">
  <div class="row">
    <div class="col-xs-4">
      <%= form_with model:@genre, url: admin_genre_path, local:true do |f| %>
        <h2>ジャンル編集</h2>
        <p>
          <span>ジャンル名</span>
          <span><%= f.text_field :name %></span>
          <span><%= f.submit "変更を保存"%></span>
        </p>
      <% end %>
    </div>
  </div>
</div>