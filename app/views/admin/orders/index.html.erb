<div class="container">
  <div class="row">
    <div class="col-xs-12">
      <h2>注文履歴一覧</h2>
      <table class="table">
      	<thead>
      		<tr class = "active">
      			<th>購入日時</th>
      			<th>購入者</th>
      			<th>注文個数</th>
      			<th>注文ステータス</th>
      		</tr>
      	</thead>
      	<% @orders.each do |order| %>
        	<tbody>
        		<tr>
        			<td><%= order.created_at %></td>
        			<td><%= order.customer.last_name + order.customer.first_name %></td>
        			<td><%= order. %></td>
        			<td><%=  %></td>
        	</tbody>
      	<% end %>
      </table>
    </div>
  </div>
</div>